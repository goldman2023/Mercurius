import { DeselectDirective } from './deselect.directive';

describe('DeselectDirective', () => {
  it('should create an instance', () => {
    const directive = new DeselectDirective();
    expect(directive).toBeTruthy();
  });
});
